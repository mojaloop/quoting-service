apiVersion: v1
kind: Service
metadata:
  name: {{ template "quoting-service-api-adapter.fullname" . }}
  labels:
    app: {{ template "quoting-service-api-adapter.name" . }}
    chart: {{ template "quoting-service-api-adapter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "quoting-service-api-adapter.name" . }}
    release: {{ .Release.Name }}
